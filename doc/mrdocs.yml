# Input
source-root: ..
# Directories that contain documented source files
input:
  - ../include
# Patterns to filter out the source-files in the directories
file-patterns:
  - '*.hpp'

# Filters
include-symbols:
  - 'boost::ws_io::**'
implementation-defined:
  - 'boost::ws_io::detail'
  - 'boost::ws_io::*::detail'
inaccessible-members: never
inaccessible-bases: never

# Generator
generate: adoc
base-url: https://www.github.com/cppalliance/ws_io/blob/develop/include/

# Style
verbose: true
multipage: true
use-system-libc: true

cmake: '-DCMAKE_CXX_STANDARD=20 -DBOOST_WS_IO_BUILD_TESTS=OFF -DBOOST_WS_IO_BUILD_EXAMPLES=OFF'
